# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# When adding additional env variables, the schema in /env/schema.mjs should be updated accordingly

# Prisma
DATABASE_URL=mysql://root:${MYSQL_ROOT_PASSWORD}@94.16.105.223:${MYSQL_PORT}/messe

# Next Auth
# You can generate the secret via 'openssl rand -base64 32' on Linux
# More info: https://next-auth.js.org/configuration/options#secrets
NEXTAUTH_SECRET="secret"
NEXTAUTH_URL=http://localhost:3000

NODE_ENV=development

# Next Auth Discord Provider 
DISCORD_CLIENT_ID= 
DISCORD_CLIENT_SECRET=

#Database Local
MYSQL_ROOT_PASSWORD=secure_password
MYSQL_DATABASE=messe
MYSQL_USER=user
MYSQL_PASSWORD=password

MYSQL_PORT=4832
PHPMYADMIN_PORT=8080

#Database Company
COMPANY_MYSQL_ROOT_PASSWORD=secure_password
COMPANY_MYSQL_DATABASE=company
COMPANY_MYSQL_USER=user
COMPANY_MYSQL_PASSWORD=password

COMPANY_MYSQL_PORT=4833
COMPANY_PHPMYADMIN_PORT=8081
